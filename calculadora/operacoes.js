module.exports = {
    
    subtracao: (num1, num2) => {
      return num1 - num2;
    },

    soma: (num1, num2) => {
        return num1 + num2;
      },

    divisao: (num1, num2) => {
        return num1 / num2;
      },
  
    multiplicacao: (num1, num2) => {
      return num1 * num2;
    },

    porcentagem: (num1) => {
      return num1 / 100;
    },

    raizQuadrada: (num1) => {
      return result = Math.sqrt(num1)
    }
  };